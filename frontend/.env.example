# ── FinSight Frontend Environment Variables ──
# Copy this file to .env and fill in values.

# Backend API URL — set this to your deployed HF Spaces backend.
# When empty, the app uses the built-in mock API (demo mode).
# VITE_API_URL=https://your-username-finance-rag-api.hf.space
